{
  "name": "spaceship-css",
  "version": "1.0.0",
  "description": "My personal component library styles",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "gulp compile",
    "start": "npm run watch",
    "prewatch": "npm run compile",
    "watch": "gulp watch"
  },
  "author": "Matthew Whitt",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^3.1.0"
  }
}
